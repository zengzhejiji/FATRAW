# FATRAW
一个极简的自定义裸文件系统，用Python实现，支持单文件读写、清空、导入功能。

## 功能
- `ff`: 查看当前文件(UTF-8)
- `fmin`:查看当前文件的前十个字符(UTF-8)
- `ffb`: 查看当前文件(bytes)
- `fminb`:查看当前文件的前十个字符(bytes)
- `copy`: 复制文件
- `null`: 清空数据
- `import`: 导入任意文件(import>>写文件名+后缀)
- `exit`: 退出程序
## 使用说明
-  请先写一个aa.post作为虚拟硬盘，第一次请先用null清空，Y不能用小写。
